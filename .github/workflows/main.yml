# Copy file to planeur.net FTP server

name: Copy_Airspace_to_FTP

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: ["feature/geojson_converter"]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
# see: https://github.com/marketplace/actions/ftp-deployment
jobs:
  web-deploy:
    name: 🎉 Upload files to FTP
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2

    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.3.0
      with:
        server: ${{ secrets.FTP_SERVER }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        local-dir: "./"
        server-dir: "./"
        exclude: |
          **/.git*
          **/.git*/**
          **/bin/**
          france.cub
          README.md
        dry-run: false

  convert-to-cub:
    name: Convert france.txt to .cub
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2

    - name: ⚙️ Convert to .cub and push to repo
      run: |
        echo "Creating new .cub file"
        chmod +x ./bin/openair2cub
        ./bin/openair2cub france.txt

        # push to repo
        echo "Pushing .cub to repo"
        git config --global user.name "github-actions[bot]"
        git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"

        git add france.cub
        
        set +e  # Grep succeeds with nonzero exit codes to show results.
        git status | grep modified
        if [ $? -eq 0 ]
        then
            set -e
            git commit -m "Auto generated file"
            git pull
            git push
        else
            set -e
            echo "No changes since last run"
        fi
        

  convert-to-geojson:
    name: 🔱Convert france.txt to .geojson
    runs-on: ubuntu-latest
    steps:
    - name: Install Node
      uses: actions/setup-node@v3
      with:
        node-version: 16

    - name: 🚚 Get latest code
      uses: actions/checkout@v2

    - name:  Convert to .geojson and push to repo
      run: |
        echo "Install openaip-openair-parser"
        mkdir openaip-openair-parser
        cd openaip-openair-parser
        git clone https://github.com/openAIP/openaip-openair-parser.git
        npm install -g @openaip/openair-parser
        ls
        node cli.js -f ../france.txt -o ../france.geojson
    
